# SecureAuth - Enterprise Security Portal

A professional authentication and security management system with MFA, logging, and admin tools.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
